# GA4 tracking fix — 22/08/2026

- Measurement ID: `G-W2HWSG9YG1`
- Google tag is now present in the server-rendered page so Google can detect it.
- Consent defaults to denied before the external Google tag loads.
- GA4 configuration and page_view events are sent only after analytics consent is granted.
- Replaced the custom array queue with Google's official `dataLayer.push(arguments)` queue format.
- WhatsApp and email click events remain enabled after consent.
- New localStorage consent key (`mva-analytics-consent-v2`) forces one fresh consent decision after deploy.
